# Byte-compiled / cache
__pycache__/
*.py[cod]
*$py.class

# Virtualenv / env
env/
venv/
.venv/
.Python

# Distribution / build
build/
dist/
*.egg-info/

# Jupyter
.ipynb_checkpoints/

# Data / datasets 
cs231n/datasets/
datasets/

# Editor / OS
.vscode/
.DS_Store
Thumbs.db

# Test / tooling caches
.pytest_cache/
.mypy_cache/

# C/Cython build artifacts (ignore generated C files and compiled extensions)
# Cython can generate .c files from .pyx; these and the compiled extensions
# (.pyd on Windows, .so on *nix) should not be checked into git.
*.c
*.cpp
*.pyd
*.so
*.dll

# Cython debug/build dirs
cython_debug/

# Keep local build folders ignored (redundant if already present elsewhere)
build/